#include <iostream>
	#include <cmath> // math 함수
	#include <string>

	using namespace std;

	int main() 
	{
		int index = 3;
		do
		{
			cout << index << endl;
			index++;
		} while (index <= 5);
		

		return 0;
	}
